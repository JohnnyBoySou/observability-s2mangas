# Railway Deployment Environment Configuration for Sentry
# This file contains environment variables specifically for Railway deployment
# Copy these to your Railway environment variables

# Required: Generate a secret key with at least 32 characters
# Use: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SENTRY_SECRET_KEY=your_32_character_minimum_secret_key_here_replace_this

# Database Configuration - Use Railway PostgreSQL addon
# Set these to your Railway PostgreSQL connection details
SENTRY_POSTGRES_HOST=${{Postgres.PGHOST}}
SENTRY_POSTGRES_PORT=${{Postgres.PGPORT}}
SENTRY_DB_NAME=${{Postgres.PGDATABASE}}
SENTRY_DB_USER=${{Postgres.PGUSER}}
SENTRY_DB_PASSWORD=${{Postgres.PGPASSWORD}}

# Redis Configuration - Use Railway Redis addon  
# Railway provides REDIS_PRIVATE_URL which is parsed by sentry.conf.py
# The sentry.conf.py file will extract host, port, and db from the URL
# No need to set individual Redis variables - they're handled by the config file
# Railway will provide: REDIS_PRIVATE_URL=redis://host:port/db
# This is automatically parsed and configured in sentry.conf.py

# Web Configuration
SENTRY_WEB_HOST=0.0.0.0
SENTRY_WEB_PORT=$PORT
SENTRY_USE_SSL=1
SENTRY_SINGLE_ORGANIZATION=1

# Email Configuration (Optional - configure as needed)
SENTRY_EMAIL_HOST=smtp.gmail.com
SENTRY_EMAIL_PORT=587
SENTRY_EMAIL_USER=your_email@gmail.com
SENTRY_EMAIL_PASSWORD=your_app_password
SENTRY_SERVER_EMAIL=noreply@yourdomain.com

# Railway Proxy Configuration
SENTRY_SECURE_PROXY_SSL_HEADER=HTTP_X_FORWARDED_PROTO,https
SENTRY_USE_X_FORWARDED_HOST=true